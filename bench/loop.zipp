// Sum 1..=50_000_000 (result fits in both i64 and JS f64, so they match).
fn main(): i64 {
    let total = 0;
    let i = 1;
    while (i <= 50000000) {
        total += i;
        i += 1;
    }
    print(total);
    return total;
}
