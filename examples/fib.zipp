// Recursive Fibonacci — PLAN.md's Phase-0 flagship. Exercises functions,
// recursion, calls, and control flow.  fib(10) = 55.
fn fib(n: i64): i64 {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main(): i64 {
    let r = fib(10);
    print(r);
    return r;
}
