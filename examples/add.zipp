// Smallest demo — mostly Const/Mul/Add steps, so almost the whole trace is
// zk-constrained. `zipp run --prove examples/add.zipp`.
fn main(): i64 {
    let a = 7;
    let b = 6;
    let c = a * b + 3;
    print(c);
    return c;
}
