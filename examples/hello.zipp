// Strings: literals, concatenation, string-typed params/returns, len().
fn greet(name: str): str {
    return "Hello, " + name + "!";
}

fn main(): i64 {
    let msg = greet("ZIPP");
    print(msg);
    print("message length:");
    print(len(msg));
    return 0;
}
