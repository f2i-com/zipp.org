[package]
name = "zipp-js"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ZIPP dynamic JavaScript engine (Lane 3) — runs untyped JS via a tree-walking interpreter over the oxc AST"

[dependencies]
oxc_allocator = "0.126.0"
oxc_ast = "0.126.0"
oxc_parser = "0.126.0"
oxc_span = "0.126.0"
oxc_syntax = "0.126.0"
