[package]
name = "zipp-gguf"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "GGUF container reading over a byte buffer, without std."

[dependencies]
zipp-quants.workspace = true

[features]
std = ["zipp-quants/std"]
