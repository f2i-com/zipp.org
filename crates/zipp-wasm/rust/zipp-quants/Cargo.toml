[package]
name = "zipp-quants"
version = "0.0.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "ggml block-quantization formats, without std, for WebAssembly."

[dependencies]
half = { version = "2.4", default-features = false }

[features]
std = []
