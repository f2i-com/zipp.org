# 0.1.0: new standalone integration experiment

All files in this archive are additions. No existing private repository files were
modified. No patch against an unseen repository is claimed.

Added a Python Graph/Tensor API, data-only graph validator, reference JS backend,
freestanding C/WASM backend, WebGPU compute source, WebGL2 fragment-compute source,
ZIPP queue handler and generation-scoped adapter, guest callback/Promise helpers,
four Python examples, a local browser demo with a compute Worker and numerical
backend checks, native Python transport demo, test suites, and implementation docs.

See docs/VALIDATION.md for what ran and what remains unverified.
