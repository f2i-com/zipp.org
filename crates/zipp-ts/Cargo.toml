[package]
name = "zipp-ts"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ZIPP OPTIONAL TypeScript frontend — parse real .ts with oxc, lower the sound subset to ZIPP's AST"

[dependencies]
oxc_allocator = "0.126.0"
oxc_ast = "0.126.0"
oxc_parser = "0.126.0"
oxc_span = "0.126.0"
oxc_syntax = "0.126.0"
zippc = { path = "../zippc" }
