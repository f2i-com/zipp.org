[package]
name = "zippc"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ZIPP compiler core + register VM (lexer, parser, sound-subset checker, bytecode, interpreter)"

[dependencies]
